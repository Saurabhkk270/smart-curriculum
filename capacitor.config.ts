import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.college.attendance',
  appName: 'College Attendance QR',
  webDir: 'dist'
};

export default config;
